rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Locked down by default - requires authenticated user
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
