<Page x:Class="Biome.Desktop.App.Pages.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    mc:Ignorable="d" 
    d:DesignHeight="600" d:DesignWidth="900"
    Title="Dashboard"
    PreviewMouseWheel="OnRootMouseWheel">

    <Grid Margin="24" VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <ui:TextBlock FontTypography="TitleLarge" Text="Console" />
            <ui:TextBlock FontTypography="Body" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="Real-time synchronization status." />
        </StackPanel>

        <ScrollViewer x:Name="ContentScroll"
                  Grid.Row="1"
                  VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled"
                  Padding="0,0,16,0"
                  Focusable="False"
                  CanContentScroll="False"
                  Background="Transparent"
                  PreviewMouseWheel="OnContentMouseWheel">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" MinWidth="300"/>
                    <ColumnDefinition Width="24"/>
                    <ColumnDefinition Width="3*" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" VerticalAlignment="Top">
                    <ui:CardAction Icon="Send24" Click="OnSendClicked" Margin="0,0,0,16">
                        <StackPanel>
                            <ui:TextBlock FontTypography="BodyStrong" Text="Send Clipboard" />
                            <ui:TextBlock FontTypography="Caption" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="Push current content to devices" />
                        </StackPanel>
                    </ui:CardAction>

                    <ui:TextBlock Text="Status" FontTypography="BodyStrong" Margin="0,12,0,8" />

                    <ui:Card Margin="0,0,0,8" Padding="12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <ui:SymbolIcon Symbol="CheckmarkCircle24" FontSize="20" Foreground="{DynamicResource SystemFillColorSuccessBrush}"/>
                            <StackPanel Grid.Column="1" Margin="12,0,0,0">
                                <ui:TextBlock Text="Last Share" FontTypography="Caption" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                <ui:TextBlock x:Name="LastShareText" Text="Waiting for action..." FontTypography="Body"/>
                            </StackPanel>
                        </Grid>
                    </ui:Card>

                    <ui:Card Margin="0,0,0,8" Padding="12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <ui:SymbolIcon Symbol="Laptop24" FontSize="20"/>
                            <StackPanel Grid.Column="1" Margin="12,0,0,0">
                                <ui:TextBlock Text="This Device" FontTypography="Caption" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                <ui:TextBlock Text="Online (Desktop)" FontTypography="Body"/>
                            </StackPanel>
                        </Grid>
                    </ui:Card>
                </StackPanel>

                <ui:Card Grid.Column="2" VerticalAlignment="Stretch" VerticalContentAlignment="Top" Padding="0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <Border BorderBrush="{DynamicResource SurfaceStrokeColorDefaultBrush}" BorderThickness="0,0,0,1" Padding="16,12">
                            <ui:TextBlock Text="Recent Activity" FontTypography="BodyStrong" />
                        </Border>

                        <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Opacity="0.6" Margin="0,32,0,0">
                            <ui:SymbolIcon Symbol="Sleep24" FontSize="48" Margin="0,0,0,12" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                            <ui:TextBlock Text="No recent transfers" FontTypography="Body" Foreground="{DynamicResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </ui:Card>
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>